# Balanced LoRA — good default for general instruction tuning.
r: 16
alpha: 32
dropout: 0.05
bias: "none"
task_type: "CAUSAL_LM"
target_modules:
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  - gate_proj
  - up_proj
  - down_proj
